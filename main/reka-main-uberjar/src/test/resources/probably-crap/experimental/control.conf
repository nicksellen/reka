

select :key {

	first {
		:request.params.name
		:results.result
		:results.results[0]
	}
	
	default some value
}

GET /people/:name {
	
}

GET /:name([0-9]+)/apples {

	add {
	
		things {
			to my map 
		}
		
		the prefix helps {
			perhaps?
		}
		
		can I pull out stuff from {
			the-data :like-this
		}
	
	}

	when {
	
		present :name {
		
			// run this stuff
		
		}
		
		empty :request.params.name {
			// run this
		}
		
		otherwise {
			
			// run this stuff
			
		}
		
	}

}